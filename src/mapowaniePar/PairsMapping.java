package mapowaniePar;

import java.util.HashMap;
import java.util.Map;
import java.util.Scanner;

/** Klasa Scanner znajduje się w pakiecie java. util. Oznacza to tyle,
 że jeżeli chcemy z niej skorzystać, to musimy ją na samym początku zaimportować. Scaner zaczynasz z dużej, bo to klasa*/
public class PairsMapping {

    public static void main(String[] args) {
    }
}

/**
 Map<String, String> pairs = new HashMap<>();
 Scanner input = new Scanner(System.in);
 String name1, name2;

 while(true) {
 System.out.println("Podaj imie partnera, lub wpisz koniec: ");
 name1 = input.next();
 if (name1.equals("-") || name1.equals("koniec")) {
 break;
 }
 System.out.println("Podaj imie partnerki: ");
 name2 = input.next();
 if (name2.equals("-")) {
 break;
 }
 pairs.put(name1, name2);
 }

 System.out.println("Podaj imie partnera do sprawdzenia: ");
 name1 = input.next();
 System.out.println("Partnerka dla " + name1 + " to " + pairs.get(name1));

 */
